# Test: db-method-proof-check

Input: a learned candidate algorithm claims exactness, but the update buffer, unseen-group cover, tie order, and fallback use inconsistent names across the definition, pseudocode, and proof.
Expected: assumption gaps + notation map + full-path dependency findings + compact complexity statement + rewrite suggestions.
Failure mode: passes theorem wording because each isolated proof paragraph looks plausible.
