# Test: db-method-proof-check

Input: a learned candidate algorithm claims exactness, but the update buffer, unseen-group cover, tie order, and fallback use inconsistent names across the definition, pseudocode, and proof.
Expected: assumption gaps + notation-budget map + reader-executable build/query/update/verification path + full-path dependency findings + compact complexity statement.
Failure mode: passes theorem wording because each isolated paragraph looks plausible or preserves symbols that have no later operation or proof responsibility.
