# Test: db-writing-copilot

Input: rough Introduction plus an advisor PDF thread saying the dynamic-workload failure should be the topic sentence, the method should be introduced explicitly, and two method paragraphs are too long.
Expected: comment-thread map + reverse outline + paragraph-function audit + paragraph-role rewrite + claim-evidence map + global follow-through and clarity risks.
Failure mode: edits highlighted sentences locally, adds defensive replies, or preserves paragraphs that do not advance a claim, mechanism, evidence, or boundary.
