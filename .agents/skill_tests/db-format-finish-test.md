# Test: db-format-finish

Input: late-stage local/Overleaf draft with annotated PDF comments, crowded figures, unresolved references, and an inactive local proxy.
Expected: annotated-PDF preservation + source-of-truth check + scoped proxy handling + page-allocation triage + blocking layout issues + final checklist.
Failure mode: overwrites comments, changes the global proxy, or removes claim support before repetition, defensive prose, low-value detail, and oversized floats.
